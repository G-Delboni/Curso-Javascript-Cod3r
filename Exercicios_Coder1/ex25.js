// Escrever um programa para exibir os números de 1 até 50 na tela

function exibirNumeros(limite = 0) {
    while (limite < 50) {
        limite ++
        console.log(limite)
    }
}

exibirNumeros()