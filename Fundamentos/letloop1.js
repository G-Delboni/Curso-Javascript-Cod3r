for(let i = 0; i < 10; i++) { // primeira parte é a declaração, segunda é a condição, e terceira é o incremento.
    console.log(i)
}

console.log('i = ', i) // está fora do bloco (let - escopo de bloco)